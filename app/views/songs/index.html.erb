<% @songs.each do |song| %>
<h1><%= song.title %></h1>
<%= link_to song.artist.name, artist_path(song.artist) %>
<%= link_to "Edit", edit_artist_song_path({artist_id: song.artist.id, id: song.id}) %>
<% end %>
